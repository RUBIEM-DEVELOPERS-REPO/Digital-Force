# Digital Force Backend Dependencies
# ─────────────────────────────────────────────────────────

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0           # PostgreSQL async driver
aiosqlite==0.20.0         # SQLite async (dev fallback)
alembic==1.14.0           # DB migrations

# Config & Validation
pydantic==2.10.0
pydantic-settings==2.6.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.3

# ─────────────────────────────────────────────────────────
# AI / Agent Layer
# ─────────────────────────────────────────────────────────
langchain==0.3.13
langchain-core==0.3.28
langchain-groq==0.2.3
langgraph==0.2.60
langchain-openai==0.2.14  # GPT-4o for complex reasoning
openai==1.58.1

# LLM Clients
groq==0.13.0
httpx==0.28.1

# ─────────────────────────────────────────────────────────
# RAG / Memory
# ─────────────────────────────────────────────────────────
qdrant-client==1.12.1
sentence-transformers==3.3.1
transformers==4.47.0
torch==2.5.1              # CPU version — change to cu121 for GPU

# ─────────────────────────────────────────────────────────
# Document Parsing (Multimodal RAG)
# ─────────────────────────────────────────────────────────
pymupdf==1.25.1            # PDF parsing (fitz)
python-docx==1.1.2         # Word documents
unstructured==0.16.11      # Universal document parser
pillow==11.0.0             # Image processing
opencv-python-headless==4.10.0.84  # Image/video processing
openai-whisper==20240930   # Audio/video transcription
yt-dlp==2024.12.13         # YouTube + video download
pandas==2.2.3              # CSV / Excel processing
openpyxl==3.1.5           # Excel support for pandas
playwright==1.49.0         # URL scraping (async)
beautifulsoup4==4.12.3     # HTML parsing
lxml==5.3.0               # HTML/XML parser

# Image ML (CLIP for image embeddings)
ftfy==6.3.2               # Text cleaning for CLIP
regex==2024.11.6

# ─────────────────────────────────────────────────────────
# Background Workers
# ─────────────────────────────────────────────────────────
celery==5.4.0
redis==5.2.1
kombu==5.4.2

# ─────────────────────────────────────────────────────────
# Publishing / Social Media APIs
# ─────────────────────────────────────────────────────────
requests==2.32.3           # Sync HTTP (for Buffer, Facebook)
httpx==0.28.1              # Async HTTP

# ─────────────────────────────────────────────────────────
# Email
# ─────────────────────────────────────────────────────────
emails==0.6                # Email sending
jinja2==3.1.4             # Email HTML templates

# ─────────────────────────────────────────────────────────
# Media / Image Generation
# ─────────────────────────────────────────────────────────
boto3==1.35.80             # S3 / Cloudflare R2 storage
aiofiles==24.1.0           # Async file operations

# ─────────────────────────────────────────────────────────
# Utilities
# ─────────────────────────────────────────────────────────
python-dotenv==1.0.1
python-slugify==8.0.4
uuid==1.30
orjson==3.10.12            # Fast JSON
arrow==1.3.0               # Better datetime handling
tenacity==9.0.0            # Retry logic
structlog==24.4.0          # Structured logging
